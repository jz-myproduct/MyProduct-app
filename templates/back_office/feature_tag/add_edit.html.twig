{% extends "back_office/base.html.twig" %}

{% block content %}
        <h1 class="h3 mb-5">Přidat/upravit tag</h1>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

        {{ form(form) }}
{% endblock %}