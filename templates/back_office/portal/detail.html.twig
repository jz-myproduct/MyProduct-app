{% extends "back_office/base.html.twig" %}

{% block content %}
    <h1 class="h3 mb-4">Portál</h1>

    <div id="accordion">
        <div class="card">
            <div class="card-header" id="headingAbout">
                <h5 class="mb-0">
                    <button class="btn btn-link" data-toggle="collapse" data-target="#collapseAbout" aria-expanded="true" aria-controls="collapseAbout">
                        Co je portál?
                    </button>
                </h5>
            </div>

            <div id="collapseAbout" class="collapse" aria-labelledby="headingAbout" data-parent="#accordion">
                <div class="card-body">
                    Lorem ipsum dolor sit amet, consectetuer adipiscing elit. Fusce suscipit libero eget elit. Nunc auctor. In laoreet, magna id viverra tincidunt, sem odio bibendum justo, vel imperdiet sapien wisi sed libero. Aliquam erat volutpat. Et harum quidem rerum facilis est et expedita distinctio. Maecenas aliquet accumsan leo. Aliquam erat volutpat. Integer vulputate sem a nibh rutrum consequat. Proin mattis lacinia justo. In sem justo, commodo ut, suscipit at, pharetra vitae, orci. Curabitur ligula sapien, pulvinar a vestibulum quis, facilisis vel sapien.
                </div>
            </div>
        </div>
        <div class="card">
            <div class="card-header" id="headingSettings">
                <h5 class="mb-0">
                    <button class="btn btn-link collapsed" data-toggle="collapse" data-target="#collapseSettings" aria-expanded="false" aria-controls="collapseSettings">
                        Nastavení
                        {% if portal.display %}
                            <span class="badge badge-success">Portál je aktivní</span>
                        {% else %}
                            <span class="badge badge-danger">Portál není aktivní</span>
                        {% endif %}
                    </button>
                </h5>
            </div>
            <div id="collapseSettings" class="collapse {% if not portal.display %}show{% endif %}" aria-labelledby="headingSettings" data-parent="#accordion">
                <div class="card-body">
                    {{ form(form) }}
                </div>
            </div>
        </div>

        {% if portal.display %}
        <div class="card">
            <div class="card-header" id="headingShare">
                <h5 class="mb-0">
                    <button class="btn btn-link collapsed" data-toggle="collapse" data-target="#collapseShare" aria-expanded="false" aria-controls="collapseShare">
                        Sdílet portál s uživateli
                    </button>
                </h5>
            </div>
            <div id="collapseShare" class="collapse show" aria-labelledby="headingShare" data-parent="#accordion">
                <div class="card-body">
                    <p><a href="{{ portalPublicUrl }}" class="btn btn-outline-primary" target="_blank" role="button">
                            Zobrazit portál</a></p>
                    <label>Sdílet portál:</label>
                    <input type="text" class="form-control" value="{{ portalPublicUrl }}" disabled="disabled" />
                </div>
            </div>
        </div>
        {% endif %}
    </div>

    {% if portal.display %}

        {% include('shared_components/portal_tabs.html.twig') %}

        {% include('shared_components/portal_features_cards.html.twig') %}

    {% endif %}
{% endblock %}