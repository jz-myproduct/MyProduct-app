{% extends "back_office/base.html.twig" %}

{% block content %}
    {% include('back_office/components/feature_header.html.twig') %}
    
    {%  include('back_office/components/feature_tabs.html.twig') %}

    {{ form_start(form) }}

      {{ form_row(form.name) }}
      {{ form_row(form.description) }}
      {{ form_row(form.state) }}
      {{ form_row(form.display) }}

      <p class="font-weight-bold">Obrázek</p>

      {% if portalFeature %}
          <p class="pb-3"><img src="{{ asset(upload_directory ~ '/' ~ portalFeature.image.name) }}"
                  alt="{{ portalFeature.name }}" style="max-width:500px" /></p>
          <a href="{{ path('bo_feature_portal_image_delete', {'company_slug':app.user.slug, 'feature_id':feature.id, 'file_id':portalFeature.image.id}) }}">smazat</a>
          {#
          <p><a href="{{ path('bo_feature_portal_image_delete', {'company_slug':app.user.slug, 'feature_id':portalFeature.feature.id, 'file_id':portalFeature.image}) }}">Odebrat obrázek</a></p>
          #}
          <label>Nahrát jiný obrázek</label>
      {% endif %}
      {{ form_row(form.image) }}


      {{ form_row(form.save) }}

    {{ form_end(form) }}

{% endblock %}