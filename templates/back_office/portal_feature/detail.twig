{% extends "back_office/base.html.twig" %}
{% block title %}
    {{ 'register_action'|trans }}
{% endblock %}
{% block content %}
    {{ 'home_title'|trans }}
    {% if app.user %}
        <div class="mb-3">
            Administrace<br />
            You are logged in as {{ app.user.username }}, <a href="{{ path('logout') }}">Logout</a>
        </div>
    {% endif %}
    <h3>Tady bude nastavení portálu</h3>
    {{ form(form) }}
{% endblock %}